import 'package:flutter/material.dart';

class PayCommission extends StatelessWidget {
  const PayCommission({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
